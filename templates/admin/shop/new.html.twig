{% extends 'admin.html.twig' %}

{% block title %}Nouvelle Boutique{% endblock %}

{% block body %}
<div class="card mb-4">
    <div class="card-header d-flex justify-content-between align-items-center">
        <h5 class="mb-0">Créer une nouvelle boutique</h5>
        <a href="{{ path('admin_shop_index') }}" class="btn btn-label-secondary">
            <i class="mdi mdi-arrow-left me-1"></i> Retour
        </a>
    </div>
    <div class="card-body">
        {% include 'admin/shop/_form.html.twig' with {'button_label': 'Créer'} %}
    </div>
</div>
{% endblock %}