<nav class="navbar fixed-bottom navbar-dark" style="background-color: #121212; border-top: 1px solid #2d2d2d;">
    <div class="container">
        <div class="d-flex justify-content-around w-100 py-1">
            <!-- Accueil -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route in ['app_main', 'app_dashboard'] ? 'active' }}" 
               href="{{ path('app_main') }}">
                <div class="nav-icon-wrapper">
                    <i class="bx bxs-home fs-5"></i>
                </div>
                <span class="nav-text mt-1">Accueil</span>
            </a>

            <!-- Boutiques -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_dashboard' ? 'active' }}" 
               href="{{ path('app_dashboard') }}">
                <div class="nav-icon-wrapper">
                    <i class="bx bxs-store fs-5"></i>
                </div>
                <span class="nav-text mt-1">Boutiques</span>
            </a>

            <!-- Support -->
            <a class="nav-link d-flex flex-column align-items-center" 
               href="#">
                <div class="nav-icon-wrapper">
                    <i class="bx bxs-chat fs-5"></i>
                </div>
                <span class="nav-text mt-1">Support</span>
            </a>

            <!-- Profil -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_profile' ? 'active' }}" 
               href="{{ path('app_profile') }}">
                <div class="nav-icon-wrapper">
                    <i class="bx bxs-user fs-5"></i>
                </div>
                <span class="nav-text mt-1">Profil</span>
            </a>
        </div>
    </div>
</nav>

<style>
    .navbar.fixed-bottom {
        height: 72px;
        padding: 0;
        backdrop-filter: blur(12px);
        box-shadow: 0 -4px 20px rgba(0, 0, 0, 0.2);
    }

    .nav-link {
        color: #888 !important;
        opacity: 0.8;
        transition: all 0.25s ease;
        padding: 12px 14px;
        position: relative;
        flex: 1;
        max-width: 90px;
    }

    .nav-link.active {
        color: #FFA500 !important;
        opacity: 1;
        transform: translateY(-6px);
    }

    .nav-link.active .nav-icon-wrapper {
        background: rgba(255, 165, 0, 0.15);
        transform: scale(1.15);
    }

    .nav-icon-wrapper {
        width: 40px;
        height: 40px;
        border-radius: 12px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    }

    .nav-text {
        font-size: 0.7rem;
        font-weight: 500;
        letter-spacing: 0.3px;
    }

    @media (max-width: 576px) {
        .navbar.fixed-bottom {
            height: 68px;
        }
        
        .nav-link {
            padding: 8px 10px;
            max-width: 80px;
        }
        
        .nav-icon-wrapper {
            width: 36px;
            height: 36px;
        }
        
        body {
            padding-bottom: 80px;
        }
    }

    body {
        padding-bottom: 84px;
    }
</style>