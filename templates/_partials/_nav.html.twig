<nav class="navbar fixed-bottom navbar-dark" style="background: rgba(18, 18, 18, 0.95); border-top: 1px solid #2d2d2d;">
    <div class="container">
        <div class="d-flex justify-content-around w-100 py-1">
            <!-- Accueil -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_dashboard' ? 'active' }}" 
               href="{{ path('app_dashboard') }}">
                <div class="nav-icon-wrapper">
                    <div class="nav-icon-bg"></div>
                    <i class="bx bxs-home fs-5 position-relative"></i>
                </div>
                <span class="nav-text mt-1">Accueil</span>
                <div class="nav-indicator"></div>
            </a>

            <!-- Boutiques -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_shop_index' or app.current_route == 'app_shop' ? 'active' }}" 
               href="{{ path('app_shop_index') }}">
                <div class="nav-icon-wrapper">
                    <div class="nav-icon-bg"></div>
                    <i class="bx bxs-store fs-5 position-relative"></i>
                </div>
                <span class="nav-text mt-1">Boutiques</span>
                <div class="nav-indicator"></div>
            </a>

            <!-- Support -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_faq' ? 'active' }}" 
               href="{{ path("app_faq") }}">
                <div class="nav-icon-wrapper">
                    <div class="nav-icon-bg"></div>
                    <i class="bx bxs-chat fs-5 position-relative"></i>
                </div>
                <span class="nav-text mt-1">Support</span>
                <div class="nav-indicator"></div>
            </a>

            <!-- Profil -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_profile' or app.current_route == 'app_user_settings'  ? 'active' }}" 
               href="{{ path('app_profile') }}">
                <div class="nav-icon-wrapper">
                    <div class="nav-icon-bg"></div>
                    <i class="bx bxs-user fs-5 position-relative"></i>
                </div>
                <span class="nav-text mt-1">Profil</span>
                <div class="nav-indicator"></div>
            </a>
        </div>
    </div>
</nav>

<style>
    .navbar.fixed-bottom {
        height: 80px;
        padding: 0;
        background: linear-gradient(to top, rgba(10, 10, 10, 0.98) 0%, rgba(20, 20, 20, 0.92) 100%) !important;
        backdrop-filter: blur(16px) saturate(180%);
        -webkit-backdrop-filter: blur(16px) saturate(180%);
        box-shadow: 0 -8px 32px rgba(0, 0, 0, 0.3);
        border-top: 1px solid rgba(255, 255, 255, 0.05);
        z-index: 9999;
    }

    .nav-link {
        color: #a0a0a0 !important;
        transition: all 0.35s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        padding: 12px 14px;
        position: relative;
        flex: 1;
        max-width: 96px;
        transform: translateY(0);
        overflow: hidden;
    }

    .nav-link.active {
        color: #fff !important;
        transform: translateY(-8px);
    }

    .nav-link.active .nav-text {
        opacity: 1;
        transform: translateY(0);
    }

    .nav-link.active .nav-icon-wrapper {
        transform: translateY(-10px) scale(1.1);
    }

    .nav-link.active .nav-icon-bg {
        opacity: 1;
        background: radial-gradient(circle, rgba(255,165,0,0.3) 0%, rgba(255,165,0,0) 70%);
    }

    .nav-link.active .nav-indicator {
        width: 24px;
        background: linear-gradient(90deg, #FFA500, #FF8C00);
        box-shadow: 0 0 12px rgba(255, 165, 0, 0.4);
    }

    .nav-icon-wrapper {
        width: 44px;
        height: 44px;
        border-radius: 14px;
        display: flex;
        align-items: center;
        justify-content: center;
        transition: all 0.4s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        position: relative;
    }

    .nav-icon-bg {
        position: absolute;
        width: 100%;
        height: 100%;
        border-radius: 14px;
        opacity: 0;
        transition: all 0.3s ease;
    }

    .nav-text {
        font-size: 0.7rem;
        font-weight: 500;
        letter-spacing: 0.3px;
        transition: all 0.3s ease;
        opacity: 0.8;
        transform: translateY(4px);
    }

    .nav-indicator {
        position: absolute;
        bottom: 8px;
        left: 50%;
        transform: translateX(-50%);
        height: 3px;
        width: 0;
        border-radius: 3px;
        transition: all 0.3s cubic-bezier(0.68, -0.55, 0.265, 1.55);
        background: #FFA500;
    }

    .bx {
        transition: all 0.3s ease;
    }

    .nav-link.active .bx {
        color: #FFA500;
        text-shadow: 0 0 8px rgba(255, 165, 0, 0.4);
    }

    @media (max-width: 576px) {
        .navbar.fixed-bottom {
            height: 72px;
        }
        
        .nav-link {
            padding: 8px 10px;
            max-width: 84px;
        }
        
        .nav-icon-wrapper {
            width: 40px;
            height: 40px;
        }
        
        body {
            padding-bottom: 80px;
        }
    }

    body {
        padding-bottom: 88px;
    }
</style>