{% extends 'emails/base_email.html.twig' %}

{% block content %}
  <h1 style="color: #2d3748; font-size: 24px; margin-bottom: 20px;"><i class="fas fa-coins" style="color: #f6ad55; margin-right: 10px;"></i> Instructions pour votre dépôt</h1>

  <p style="margin-bottom: 16px;">
    Bonjour,<br />
    Voici les détails pour effectuer votre dépôt de <strong>{{ amount|number_format(2) }} USD</strong> en {{ currency|upper }}.
  </p>

  <div style="background: #f7fafc; padding: 20px; border-radius: 8px; margin-bottom: 20px; border-left: 4px solid #68d391;">
    <h2 style="color: #2d3748; font-size: 18px; margin-bottom: 10px;"><i class="fas fa-wallet" style="color: #4fd1c5; margin-right: 8px;"></i> Adresse de destination</h2>
    <div style="background: white; padding: 10px; border-radius: 4px; font-family: monospace; font-size: 16px; word-break: break-all;">{{ address }}</div>

    <h2 style="color: #2d3748; font-size: 18px; margin-top: 20px; margin-bottom: 10px;"><i class="fas fa-address-card" style="color: #4fd1c5; margin-right: 8px;"></i> Votre adresse source</h2>
    <div style="background: white; padding: 10px; border-radius: 4px; font-family: monospace; font-size: 16px; word-break: break-all;">{{ source_address }}</div>

    <div style="text-align: center; margin: 20px 0;">
      <img src="{{ qr_code }}" alt="QR Code" style="max-width: 200px; border: 1px solid #e2e8f0; border-radius: 8px;" />
    </div>

    <p style="margin-bottom: 10px;">
      <strong><i class="fas fa-money-bill-wave" style="color: #f6ad55; margin-right: 8px;"></i> Montant exact :</strong> {{ amount|number_format(2) }} USD<br />
      <strong><i class="fas fa-coins" style="color: #f6ad55; margin-right: 8px;"></i> Cryptomonnaie :</strong> {{ currency|upper }}<br />
      <strong><i class="fas fa-clock" style="color: #f6ad55; margin-right: 8px;"></i> Expire le :</strong> {{ expiry|date('d/m/Y à H:i') }}
    </p>
  </div>

  <div style="background: #fffaf0; padding: 15px; border-left: 4px solid #ed8936; margin-bottom: 20px;">
    <h3 style="color: #dd6b20; font-size: 16px; margin-top: 0;"><i class="fas fa-exclamation-triangle" style="margin-right: 8px;"></i> Important</h3>
    <ul style="margin-bottom: 0; padding-left: 20px;">
      <li>Envoyez uniquement {{ currency|upper }} à cette adresse</li>
      <li>Le montant doit être exactement {{ amount|number_format(2) }} USD</li>
      <li>Le dépôt expirera à {{ expiry|date('H:i') }}</li>
      <li>Les fonds doivent provenir de votre adresse: {{ source_address }}</li>
    </ul>
  </div>

  <p style="margin-bottom: 16px;">Si vous avez des questions, n'hésitez pas à répondre à cet email.</p>

  <p style="margin-bottom: 0;">
    Cordialement,<br />
    L'équipe {{ app_name }}
  </p>
{% endblock %}
