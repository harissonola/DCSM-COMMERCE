<nav class="navbar fixed-bottom navbar-dark" style="background-color: #1a1a1a; border-top: 1px solid #2d2d2d;">
    <div class="container">
        <div class="d-flex justify-content-around w-100 py-2">
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route in ['app_main', 'app_dashboard'] ? 'active' }}" 
               href="{{ path('app_main') }}">
                <i class="bx bxs-home fs-5 mb-1"></i>
                <span class="nav-text">Accueil</span>
            </a>

            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_dashboard' ? 'active' }}" 
               href="{{ path('app_dashboard') }}">
                <i class="bx bxs-wallet fs-5 mb-1"></i>
                <span class="nav-text">Boutiques</span>
            </a>

            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == '#' ? 'active' }}" 
               href="#">
                <i class="bx bxs-headset fs-5 mb-1"></i>
                <span class="nav-text">Support</span>
            </a>

            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_profile' ? 'active' }}" 
               href="{{ path('app_profile') }}">
                <i class="bx bxs-cog fs-5 mb-1"></i>
                <span class="nav-text">Mon Profil</span>
            </a>
        </div>
    </div>
</nav>

<style>
    .navbar.fixed-bottom {
        height: auto; /* Hauteur automatique selon le contenu */
        padding: 0.5rem 1rem; /* Padding horizontal ajouté */
        backdrop-filter: blur(8px);
    }
    
    .nav-link {
        color: #888 !important;
        opacity: 0.7;
        transition: all 0.3s cubic-bezier(0.4, 0, 0.2, 1);
        padding: 10px 14px 14px 14px; /* Padding augmenté en bas */
        border-radius: 8px;
        background-color: transparent;
        will-change: transform, background-color;
        margin: 0 2px; /* Espacement entre les liens */
    }

    .nav-link.active {
        color: #1e90ff !important;
        opacity: 1;
        transform: translateY(-4px); /* Effet de levée augmenté */
        background-color: rgba(30, 144, 255, 0.1);
        box-shadow: 0 2px 12px rgba(30, 144, 255, 0.1);
        padding-bottom: 16px; /* Compensation du déplacement */
    }

    /* Ajout d'un conteneur d'icône */
    .nav-link i {
        padding: 4px; /* Espace autour de l'icône */
        display: inline-block;
    }

    @media (max-width: 576px) {
        .nav-link {
            padding: 8px 10px 12px 10px; /* Ajustement mobile */
            margin: 0 1px;
        }
        
        .nav-link.active {
            padding-bottom: 14px;
        }
        
        body {
            padding-bottom: 82px; /* Augmentation pour mobile */
        }
    }

    body {
        padding-bottom: 88px; /* Augmentation générale */
        transition: padding-bottom 0.3s ease;
    }
</style>