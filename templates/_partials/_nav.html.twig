<nav class="navbar navbar-expand-lg navbar-dark fixed-top navbar-loading" id="navbar">
	<div
		class="container">
		<!-- Skeleton loader pour la marque -->
		<div class="navbar-skeleton"></div>
		<a class="navbar-brand text-primary" href="{{ path('app_dashboard') }}" data-aos-duration="2000" data-aos="slide-right" data-aos-delay="200">DCSM COMMERCE</a>
		<button class="navbar-toggler d-lg-none" type="button" data-bs-toggle="collapse" data-bs-target="#collapsibleNavId" aria-controls="collapsibleNavId" aria-label="Toggle navigation" aria-expanded="false">
			<span class="navbar-toggler-icon"></span>
		</button>
		<div class="collapse navbar-collapse" id="collapsibleNavId">
			<ul class="navbar-nav me-auto mt-2 mt-lg-0">
				<li class="nav-item" data-aos-duration="2000" data-aos="slide-up" data-aos-delay="300">
					<a class="nav-link active" href="{{ path('app_dashboard') }}" aria-current="page">Boutiques
						<span class="visually-hidden">(current)</span>
					</a>
				</li>
				<li class="nav-item">
					<a data-aos-duration="2000" data-aos="slide-down" data-aos-delay="400" class="nav-link" href="#">Produits</a>
				</li>
				<li class="nav-item dropdown">
					<a class="nav-link dropdown-toggle" href="#" id="dropdownId" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">Dropdown</a>
					<div class="dropdown-menu" aria-labelledby="dropdownId">
						<a class="dropdown-item" href="#">Action 1</a>
						<a class="dropdown-item" href="#">Action 2</a>
					</div>
				</li>
			</ul>

			{% if app.user %}
				<!-- Menu utilisateur avec avatar -->
				<ul class="navbar-nav ms-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle d-flex align-items-center" href="#" id="userDropdown" data-bs-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
							
							<img src="{{app.user.photo}}" alt="User Avatar" class="rounded-circle" width="30" height="30">


							<span class="ms-2">{{ app.user.username }}</span>
							<!-- Affichage du nom de l'utilisateur -->
						</a>
						<div class="dropdown-menu dropdown-menu-end" aria-labelledby="userDropdown">
							<a class="dropdown-item" href="{{ path('app_profile') }}">Profil</a>
							<a class="dropdown-item" href="{{ path('app_user_settings') }}">Paramètres</a>
							<a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a>
						</div>
					</li>
				</ul>
			{% endif %}


			<!-- Skeleton loader pour le champ de recherche -->
			<div class="form-control-skeleton"></div>
			<form class="d-flex my-2 my-lg-0 gap-2" id="search-form" data-turbo-action="replace" data-turbo-frame="search-results">
				<div class="form-control-skeleton" id="form-control-skeleton"></div>
				<input class="form-control me-sm-2" type="text" placeholder="Search" id="search-input"/>
				<div class="btn-skeleton" id="btn-skeleton"></div>
				<button class="btn btn-outline-warning my-2 my-sm-0" type="submit">Search</button>
			</form>
		</div>
	</div>
</nav>

<style>
	/* Navbar loader */
	.navbar {
		background-color: rgba(0, 0, 0, 0.7);
	}
</style>
