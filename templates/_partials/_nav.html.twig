<nav class="navbar fixed-bottom navbar-dark">
    <div class="container">
        <div class="d-flex justify-content-around w-100 py-2">
            <!-- Accueil -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_dashboard' ? 'active' }}" 
               href="{{ path('app_dashboard') }}">
                <div class="nav-bubble">
                    <i class="bx bxs-home"></i>
                </div>
                <span class="nav-label">Accueil</span>
            </a>

            <!-- Boutiques -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_shop_index' or app.current_route == 'app_shop' ? 'active' }}" 
               href="{{ path('app_shop_index') }}">
                <div class="nav-bubble">
                    <i class="bx bxs-store"></i>
                </div>
                <span class="nav-label">Boutiques</span>
            </a>

            <!-- Support -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_faq' ? 'active' }}" 
               href="{{ path("app_faq") }}">
                <div class="nav-bubble">
                    <i class="bx bxs-chat"></i>
                </div>
                <span class="nav-label">Support</span>
            </a>

            <!-- Profil -->
            <a class="nav-link d-flex flex-column align-items-center {{ app.current_route == 'app_profile' or app.current_route == 'app_user_settings'  ? 'active' }}" 
               href="{{ path('app_profile') }}">
                <div class="nav-bubble">
                    <i class="bx bxs-user"></i>
                </div>
                <span class="nav-label">Profil</span>
            </a>
        </div>
    </div>
</nav>

<style>
    .navbar.fixed-bottom {
        height: 88px;
        background: linear-gradient(to top, 
            rgba(40, 40, 40, 0.95) 0%, 
            rgba(30, 30, 30, 0.88) 100%);
        backdrop-filter: blur(12px) saturate(180%);
        -webkit-backdrop-filter: blur(12px) saturate(180%);
        border-top: 1px solid rgba(255, 255, 255, 0.1);
        box-shadow: 0 -15px 40px rgba(0, 0, 0, 0.25);
        padding: 0;
    }

    .nav-link {
        position: relative;
        color: #8a8a8e;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        flex: 1;
        max-width: 96px;
        padding: 12px 0;
    }

    .nav-link.active {
        color: #ffffff;
        transform: translateY(-8px);
    }

    .nav-bubble {
        width: 52px;
        height: 52px;
        border-radius: 50%;
        display: flex;
        align-items: center;
        justify-content: center;
        background: transparent;
        transition: all 0.4s cubic-bezier(0.16, 1, 0.3, 1);
        position: relative;
        z-index: 1;
    }

    .nav-link.active .nav-bubble {
        background: linear-gradient(135deg, #FF9A00, #FF5E00);
        box-shadow: 0 8px 24px rgba(255, 94, 0, 0.3);
    }

    .nav-link.active .nav-bubble::before {
        content: '';
        position: absolute;
        top: -4px;
        left: -4px;
        right: -4px;
        bottom: -4px;
        border-radius: 50%;
        background: linear-gradient(135deg, rgba(255, 154, 0, 0.4), rgba(255, 94, 0, 0.4));
        z-index: -1;
        animation: pulse 2s infinite;
    }

    .nav-link .bx {
        font-size: 1.4rem;
        transition: all 0.3s ease;
    }

    .nav-link.active .bx {
        color: white;
        transform: scale(1.1);
    }

    .nav-label {
        font-size: 0.75rem;
        font-weight: 500;
        margin-top: 6px;
        opacity: 0.8;
        transition: all 0.3s ease;
    }

    .nav-link.active .nav-label {
        opacity: 1;
        font-weight: 600;
        transform: translateY(4px);
    }

    @keyframes pulse {
        0% { transform: scale(0.95); opacity: 0.8; }
        50% { transform: scale(1.05); opacity: 0.4; }
        100% { transform: scale(0.95); opacity: 0.8; }
    }

    @media (max-width: 576px) {
        .navbar.fixed-bottom {
            height: 80px;
        }
        
        .nav-bubble {
            width: 46px;
            height: 46px;
        }
        
        .nav-link {
            max-width: 84px;
            padding: 8px 0;
        }
        
        body {
            padding-bottom: 84px;
        }
    }

    body {
        padding-bottom: 92px;
    }
</style>