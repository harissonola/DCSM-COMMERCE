{% extends 'emails/base_email.html.twig' %}

{% block content %}
  <h1 style="color: #2d3748; font-size: 24px; margin-bottom: 20px;">Dépôt confirmé</h1>

  <p style="margin-bottom: 16px;">
    Bonjour,<br />
    Votre dépôt de <strong>{{ amount|number_format(2) }} USD</strong> en {{ currency|upper }} a été confirmé et crédité sur votre compte.
  </p>

  <div style="background: #f0fff4; padding: 20px; border-radius: 8px; margin-bottom: 20px; border-left: 4px solid #48bb78;">
    <h2 style="color: #2d3748; font-size: 18px; margin-top: 0;">Détails de la transaction</h2>

    <table style="width: 100%;">
      <tr>
        <td style="padding: 5px 0; width: 120px;">
          <strong>Montant :</strong>
        </td>
        <td style="padding: 5px 0;">{{ amount|number_format(2) }} USD</td>
      </tr>
      <tr>
        <td style="padding: 5px 0;">
          <strong>Cryptomonnaie :</strong>
        </td>
        <td style="padding: 5px 0;">{{ currency|upper }}</td>
      </tr>
      {% if tx_hash %}
        <tr>
          <td style="padding: 5px 0;">
            <strong>Transaction :</strong>
          </td>
          <td style="padding: 5px 0; word-break: break-all;">{{ tx_hash }}</td>
        </tr>
      {% endif %}
    </table>
  </div>

  <p style="margin-bottom: 16px;">Vous pouvez maintenant utiliser ces fonds sur notre plateforme.</p>

  <div style="text-align: center; margin: 25px 0;">
    <a href="{{ app_url }}/profile" style="background: #4299e1; color: white; padding: 10px 20px; text-decoration: none; border-radius: 4px; display: inline-block;">Voir mon solde</a>
  </div>

  <p style="margin-bottom: 0;">
    Cordialement,<br />
    L'équipe {{ app_name }}
  </p>
{% endblock %}
