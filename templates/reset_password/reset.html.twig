{% extends 'base.html.twig' %}

{% block title %}Reinitialiser votre mot de passe{% endblock %}

{% block body %}
    <div class="container py-5 mt-5" >
      <div class="card">
        <div class="card-body">
          <h5 class="card-title">Reinitialiser votre mot de passe</h5>
          {{ form_start(resetForm) }}
          {{ form_row(resetForm.plainPassword) }}
          <button class="btn btn-primary mt-3">Reinitialiser le mot de passe</button>
      {{ form_end(resetForm) }}
        </div>
      </div>
    </div>
{% endblock %}
